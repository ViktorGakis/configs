#! /bin/bash

# on kali linux there is no need for install
# just use the command code
# it will automatically install it

sudo snap install --classic code

# while IFS= read -r extension; do
#     code --install-extension "$extension" --force
# done <"vscode_ext.txt"

